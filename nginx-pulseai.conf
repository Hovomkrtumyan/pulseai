# Nginx reverse proxy config for PulseAi
# Add this to your main nginx config or as a separate include
# If you use nginx as a reverse proxy for multiple apps on 109.75.40.220

location /PulseAi {
    proxy_pass http://localhost:3100/PulseAi;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;
    client_max_body_size 5M;
}
